import { defineConfig } from 'vite'

// Minimal Vite config without plugin to avoid peer dependency issues
export default defineConfig({
  // If you later install @vitejs/plugin-react, add it to the plugins array.
})
